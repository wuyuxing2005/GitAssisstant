# Ragas Dataset Files

Place evaluation datasets here as JSONL files named after the `dataset` field in a task config.

Example:

- task config dataset: `customer-support-v2`
- dataset file: `backend/data/eval_datasets/customer-support-v2.jsonl`

Each line should be one JSON object.
