# Docker Compose interpolation settings generated by ci/deploy.sh on the server.
# Keep this file as documentation; the real .deploy.env is recreated each deploy.

COMPOSE_PROJECT_NAME=agent-eval-staging
BACKEND_IMAGE=agent-eval-backend:latest
FRONTEND_IMAGE=agent-eval-frontend:latest
FRONTEND_PORT=80
BACKEND_PORT=8000
POSTGRES_PORT=5432
