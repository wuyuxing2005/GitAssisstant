# Agent 平台 - 后端

## 技术选型

- FastAPI
- Pydantic v2
- Ragas（预留真实评估接入）

## 能力覆盖

## 启动方式

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -e .
uvicorn gitIssueAssitant.RESTAPIAdapter.main:app --reload --port 8000
```
